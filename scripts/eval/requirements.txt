# RAG 检索评测 Python 依赖
# RAGAS 0.2.x 未给 langchain 设置版本上限，需手动锁到 0.3.x，避免拉到不兼容的
# langchain-community 0.4.x（移除了 chat_models.vertexai 等模块）。
ragas>=0.2.0,<0.3.0
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langchain-openai>=0.3.0,<0.4.0
# datasets 2.x 依赖 pyarrow.PyExtensionType，pyarrow 16+ 已移除该别名，需锁 pyarrow<16
datasets>=2.14,<3.0
pyarrow>=14.0,<16.0
pandas>=2.0,<3.0
# nltk 是 ragas.metrics 导入期的硬依赖（BleuScore 在 __post_init__ 里 import word_tokenize）
nltk>=3.8
psycopg2-binary>=2.9
python-dotenv>=1.0
